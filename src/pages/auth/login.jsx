import React, { Fragment } from 'react'
import FormLogin from '../../components/fragment/auth/formLogin'

const LoginPage = () => {
    return (
        <Fragment>
            <FormLogin />
        </Fragment>
    )
}

export default LoginPage
